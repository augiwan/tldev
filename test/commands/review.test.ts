import {runCommand} from '@oclif/test'
import {expect} from 'chai'

describe('review', () => {
  // it('runs review cmd', async () => {
  //   const {stdout} = await runCommand('review')
  //   expect(stdout).to.contain('hello world')
  // })
  // it('runs review --name oclif', async () => {
  //   const {stdout} = await runCommand('review --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
})

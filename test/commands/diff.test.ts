import {runCommand} from '@oclif/test'
import {expect} from 'chai'

describe('diff', () => {
  // it('runs diff', async () => {
  //   const {stdout} = await runCommand('diff')
  //   expect(stdout).to.contain('No changes detected')
  // })
})
